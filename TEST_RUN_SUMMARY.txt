Test run summary for NanoChemGPT
Date: 2025-10-03

Command executed:
PYTHONIOENCODING=utf-8 SPACY_MODEL=harvester/miner/ner_model/model-best python -m pytest -q

Environment:
- OS: Windows (platform win32)
- Python: 3.11.9
- pytest: 8.4.2
- Notable packages: spaCy installed locally (warning about model version), faiss present but incompatible with local NumPy (NumPy 2.x present)

What was tested:
- Executed full pytest test suite (all tests under `tests/`). This includes unit tests and integration tests.
- Integration miner test (`tests/test_miner_integration.py`) was included because `SPACY_MODEL` was set to the repository `model-best` path.

Results:
- Total collected tests: 53 items (collection failed early due to import errors)
- Run outcome: pytest collection aborted with 2 import errors during test collection.
  - Errors encountered while importing `app` (imported by tests/test_job_status.py and tests/test_transcribe_integration_pytest.py).
  - Root cause: Importing `vector_store` triggers `import faiss`, which fails with an ABI error due to NumPy 2.x vs faiss compiled for NumPy 1.x.
  - Error messages seen:
    - "ImportError: numpy.core.multiarray failed to import"
    - "A module that was compiled using NumPy 1.x cannot be run in NumPy 2.3.3... downgrade to 'numpy<2' or rebuild module."
    - "AttributeError: _ARRAY_API not found"

Key observations:
- The miner integration test itself (previously run in isolation) passes locally when spaCy and the model are available and the runtime records absolute path.
- The current local Python environment has NumPy 2.x, which is incompatible with the installed `faiss` binary wheel compiled against NumPy 1.x. That causes early import failures during test collection because `app.py` imports `vector_store` at module import time.

Recommendations / Next steps:
1. To run the full test suite locally, either:
   - Downgrade NumPy locally to a 1.x-compatible version (e.g., pip install "numpy<2") and reinstall faiss, or
   - Uninstall faiss for local runs where it's not required, or
   - Modify `app.py` / `vector_store/__init__.py` to lazily import faiss only when needed (safer for tests that don't use it).

2. CI already includes a miner-integration job that installs `numpy<2` and `faiss-cpu` — push the branch/PR so CI verifies the remote runner behavior.

3. If updating runtime imports, consider making `vector_store` import-time side effects minimal and fail gracefully when faiss is not available so unit tests that don't need vector store don't break.

Log excerpt:
- See pytest output in terminal for full traceback; primary failure points in `vector_store/__init__.py` importing faiss.

Generated by: assistant action on Oct 3, 2025
